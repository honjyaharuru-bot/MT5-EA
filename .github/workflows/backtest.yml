      - name: Sync to Google Drive
        shell: powershell
        run: |
          $dest = "$env:USERPROFILE\Google Drive\マイドライブ\MT5-Results"
          if (Test-Path $dest) {
            Copy-Item results\* $dest -Force
          }
